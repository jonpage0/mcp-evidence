SELECT 
    id,
    created_at,
    updated_at,
    name,
    details,
    phone_number,
    email,
    website_url,
    logo_url,
    passkit_venue_id,
    address_id,
    venue_type_id,
    location_id
FROM venues;